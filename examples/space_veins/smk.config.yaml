# tles_dir: "tles/"
tles_prefixes: ["iridiumNEXT", "eccentric"] # "oneweb", "satnogs" , "starlink"]
traces_dir: "traces/"

debug_mode: false
mobilities: ["sgp4", "kepler"]
itervars: [
  #"warmupPeriod", 
  #"simTimeLimit", 
  #"nodeXPos"
  ]
sat_mod_regex_base: "SatelliteExampleScenario.leo.vehicleStatistics"

coord_frames: ["itrf", "omnet"] # "wgs84"]